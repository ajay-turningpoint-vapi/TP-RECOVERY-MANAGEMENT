// The real, current TP-RMS app (Salesperson / Recovery Executive / Manager)
// lives in v3/main_v3.dart. This file exists only so the default Flutter
// entry point — `flutter run`, `flutter build apk` with no --target flag —
// launches that real app instead of an old prototype.
import 'v3/main_v3.dart' as app;

void main() => app.main();
